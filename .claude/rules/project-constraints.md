# Project Constraints

- I am using git bash as a terminal so unix commands work
- yarn is used as a package manager
- You can run `yarn typecheck` to check for typed errors
- You can run `yarn eslint . --quiet` to check for linting errors
